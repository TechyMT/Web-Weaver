[tool.poetry]
name = "test"
version = "0.1.0"
description = ""
authors = ["Mustafa <c2k21106896@ms.pict.edu>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
ai-engine = "^0.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
